<UserControl x:Class="FanucRobotServerSH.Views.PositionSysvarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1000"
             Background="#F5F5F5">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 位置编辑 -->
            <Border Style="{StaticResource CardBorder}" Grid.Column="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="位置编辑（当前位姿 / PR）"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Style="{StaticResource PrimaryButton}" Content="加载位姿 POS[1]" Command="{Binding LoadPosCommand}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="PR 索引 "/>
                        <TextBox Width="60" Text="{Binding PrIndex}"/>
                        <Button Style="{StaticResource DefaultButton}" Content="加载 PR" Command="{Binding LoadPrCommand}"/>
                    </StackPanel>

                    <TextBlock Style="{StaticResource GroupHeader}" Text="笛卡尔坐标"/>
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="X " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="1" Width="70" Text="{Binding PosX, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="2" Style="{StaticResource FieldLabel}" Text="Y " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="3" Width="70" Text="{Binding PosY, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="4" Style="{StaticResource FieldLabel}" Text="Z " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="5" Width="70" Text="{Binding PosZ, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="W " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="1" Width="70" Text="{Binding PosW, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="2" Style="{StaticResource FieldLabel}" Text="P " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="3" Width="70" Text="{Binding PosP, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="4" Style="{StaticResource FieldLabel}" Text="R " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="5" Width="70" Text="{Binding PosR, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <TextBlock Style="{StaticResource GroupHeader}" Text="关节坐标"/>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="J1 " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="1" Width="60" Text="{Binding PosJ1, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="2" Style="{StaticResource FieldLabel}" Text="J2 " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="3" Width="60" Text="{Binding PosJ2, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="4" Style="{StaticResource FieldLabel}" Text="J3 " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="5" Width="60" Text="{Binding PosJ3, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Style="{StaticResource FieldLabel}" Text="J4 " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="1" Width="60" Text="{Binding PosJ4, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="2" Style="{StaticResource FieldLabel}" Text="J5 " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="3" Width="60" Text="{Binding PosJ5, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,10,0"/>
                        <TextBlock Grid.Column="4" Style="{StaticResource FieldLabel}" Text="J6 " Margin="0,0,4,0"/>
                        <TextBox Grid.Column="5" Width="60" Text="{Binding PosJ6, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal">
                        <Button Style="{StaticResource PrimaryButton}" Content="应用到位姿 POS[1]" Command="{Binding ApplyPosCommand}"/>
                        <Button Style="{StaticResource DefaultButton}" Content="应用到 PR" Command="{Binding ApplyPrCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- 系统变量 -->
            <Border Style="{StaticResource CardBorder}" Grid.Column="1">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="系统变量"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="变量名 "/>
                        <TextBox Width="220" Text="{Binding SysvarName}"/>
                        <ComboBox Width="90" ItemsSource="{Binding SysvarTypes}" SelectedItem="{Binding SysvarType}" Margin="6,0,0,0"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadSysvarCommand}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="值 "/>
                        <TextBox Width="220" Text="{Binding SysvarValue}"/>
                        <Button Style="{StaticResource DefaultButton}" Content="写入" Command="{Binding WriteSysvarCommand}"/>
                    </StackPanel>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="读取结果（含位置类型）：" Margin="0,4,0,4"/>
                    <ItemsControl ItemsSource="{Binding SysvarResults}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F0F4F0" BorderBrush="#C8E6C9" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,2">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#1B5E20"/>
                                        <TextBlock Text="{Binding Value}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
