<UserControl x:Class="FanucRobotDemoSH.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1000"
             Background="{StaticResource AppBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- R -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="数值寄存器 R[i]"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="R["/>
                        <TextBox Width="40" Text="{Binding RIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] 读取 →"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadRCommand}"/>
                        <TextBlock Text="{Binding RResult}" FontWeight="Bold" Foreground="#2196F3" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="R["/>
                        <TextBox Width="40" Text="{Binding RWriteIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] = "/>
                        <TextBox Width="80" Text="{Binding RWriteValue}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WriteRCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- R 批量读 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="1">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource PrimaryButton}" Content="R[1]~R[10] 批量读取" Command="{Binding ReadRBatchCommand}"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" Margin="10,0,0,0" VerticalScrollBarVisibility="Disabled">
                        <TextBlock Text="{Binding RBatchResult}" VerticalAlignment="Center"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <!-- SR -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="2">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="字符串寄存器 SR[i]"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="SR["/>
                        <TextBox Width="40" Text="{Binding SrIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] 读取 →"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadSrCommand}"/>
                        <TextBlock Text="{Binding SrResult}" FontWeight="Bold" Foreground="#009688" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="SR["/>
                        <TextBox Width="40" Text="{Binding SrWriteIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] = "/>
                        <TextBox Width="200" Text="{Binding SrWriteValue}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WriteSrCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- F -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="3">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource CardTitle}" Text="标志寄存器 F[i]" VerticalAlignment="Center"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="F["/>
                    <TextBox Width="40" Text="{Binding FIndex}"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="] 读取 →"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadFCommand}"/>
                    <TextBlock Text="{Binding FResult}" FontWeight="Bold" Foreground="#E91E63" VerticalAlignment="Center" Margin="5,0,15,0"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="F["/>
                    <TextBox Width="40" Text="{Binding FWriteIndex}"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="] = "/>
                    <ComboBox Width="64" SelectedIndex="{Binding FValueIndex}">
                        <ComboBoxItem Content="ON"/>
                        <ComboBoxItem Content="OFF"/>
                    </ComboBox>
                    <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WriteFCommand}"/>
                </StackPanel>
            </Border>

            <!-- R 批量写 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="4">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource FieldLabel}" Text="起始 R["/>
                    <TextBox Width="40" Text="{Binding RWriteBatchStart}"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="] = "/>
                    <TextBox Width="240" Text="{Binding RWriteBatchValues}"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="批量写入" Command="{Binding WriteRBatchCommand}"/>
                </StackPanel>
            </Border>

            <!-- SR 批量读 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource FieldLabel}" Text="起始 SR["/>
                    <TextBox Width="40" Text="{Binding SrBatchStart}"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="] 数量:"/>
                    <TextBox Width="40" Text="{Binding SrBatchCount}"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="批量读取" Command="{Binding ReadSrBatchCommand}"/>
                    <TextBlock Text="{Binding SrBatchResult}" FontWeight="Bold" Foreground="#009688" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
