<UserControl x:Class="FanucRobotDemoSH.Views.PositionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1000"
             Background="{StaticResource AppBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 当前位置 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="当前位置 (Group 1)"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Style="{StaticResource PrimaryButton}" Content="读取世界坐标" Command="{Binding ReadWorldPosCommand}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取关节坐标" Command="{Binding ReadJointPosCommand}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取指定 UF 位置" Command="{Binding ReadUserPosCommand}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text=" UF:" VerticalAlignment="Center"/>
                        <TextBox Width="32" Text="{Binding UfNumber}"/>
                    </StackPanel>
                    <Border Background="#F5F5F5" Padding="8">
                        <TextBlock Text="{Binding PositionResult}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- PR 读取 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="1">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="位置寄存器 PR[i]"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="PR["/>
                        <TextBox Width="40" Text="{Binding PrIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] Group:"/>
                        <TextBox Width="32" Text="{Binding PrGroup}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadPrCommand}"/>
                    </StackPanel>
                    <Border Background="#F5F5F5" Padding="8">
                        <TextBlock Text="{Binding PrResult}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- PR 写入 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="2">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="PR 写入关节坐标 (UF=1, UT=1)"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="目标 PR["/>
                        <TextBox Width="40" Text="{Binding PrWriteIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="]"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WritePrCommand}"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="J1" FontSize="11"/><TextBox Text="{Binding J1}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,5,0">
                            <TextBlock Text="J2" FontSize="11"/><TextBox Text="{Binding J2}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,5,0">
                            <TextBlock Text="J3" FontSize="11"/><TextBox Text="{Binding J3}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="3" Margin="0,0,5,0">
                            <TextBlock Text="J4" FontSize="11"/><TextBox Text="{Binding J4}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4" Margin="0,0,5,0">
                            <TextBlock Text="J5" FontSize="11"/><TextBox Text="{Binding J5}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="5">
                            <TextBlock Text="J6" FontSize="11"/><TextBox Text="{Binding J6}" Padding="2"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- PR 写入世界坐标 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="3">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="PR 写入世界坐标（笛卡尔，机器人自动反算关节）"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="目标 PR["/>
                        <TextBox Width="40" Text="{Binding CartWriteIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] UF:"/>
                        <TextBox Width="32" Text="{Binding CartUf}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text=" UT:"/>
                        <TextBox Width="32" Text="{Binding CartUt}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WritePrCartesianCommand}"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="X" FontSize="11"/><TextBox Text="{Binding X}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,5,0">
                            <TextBlock Text="Y" FontSize="11"/><TextBox Text="{Binding Y}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,5,0">
                            <TextBlock Text="Z" FontSize="11"/><TextBox Text="{Binding Z}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="3" Margin="0,0,5,0">
                            <TextBlock Text="W" FontSize="11"/><TextBox Text="{Binding W}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="4" Margin="0,0,5,0">
                            <TextBlock Text="P" FontSize="11"/><TextBox Text="{Binding P}" Padding="2"/>
                        </StackPanel>
                        <StackPanel Grid.Column="5">
                            <TextBlock Text="R" FontSize="11"/><TextBox Text="{Binding R}" Padding="2"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
