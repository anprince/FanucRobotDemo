<UserControl x:Class="FanucRobotDemoSH.Views.DigitalIOView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource AppBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- DI / DO 单路 -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Style="{StaticResource CardBorder}" Grid.Column="0">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitle}" Text="DI (数字输入)"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource FieldLabel}" Text="索引 "/>
                            <TextBox Width="60" Text="{Binding DiIndex}"/>
                            <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadDiCommand}"/>
                            <TextBlock Text="{Binding DiResult}" FontWeight="Bold" Foreground="#2196F3"
                                       VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Grid.Column="1">
                    <StackPanel>
                        <TextBlock Style="{StaticResource CardTitle}" Text="DO (数字输出)"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource FieldLabel}" Text="索引 "/>
                            <TextBox Width="60" Text="{Binding DoIndex}"/>
                            <TextBlock Style="{StaticResource FieldLabel}" Text=" 值 "/>
                            <ComboBox Width="64" SelectedIndex="{Binding DoValueIndex}">
                                <ComboBoxItem Content="ON"/>
                                <ComboBoxItem Content="OFF"/>
                            </ComboBox>
                            <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WriteDoCommand}"/>
                            <Button Style="{StaticResource DefaultButton}" Content="读取" Command="{Binding ReadDoCommand}"/>
                            <TextBlock Text="{Binding DoResult}" FontWeight="Bold" Foreground="#FF9800"
                                       VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- DO 批量 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="1">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource PrimaryButton}" Content="DO[1]~DO[8] 批量读取" Command="{Binding ReadDoBatchCommand}"/>
                    <TextBlock Text="{Binding DoBatchSummary}" Style="{StaticResource FieldLabel}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>
            </Border>

            <!-- AI/AO -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource CardTitle}" Text="AI/AO (模拟信号)" VerticalAlignment="Center"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="读取 AI[1]" Command="{Binding ReadAiCommand}" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding AiResult}" FontWeight="Bold" Foreground="#9C27B0" VerticalAlignment="Center" Margin="5,0,15,0"/>
                    <TextBlock Style="{StaticResource FieldLabel}" Text="写入 AO[1]:" VerticalAlignment="Center"/>
                    <TextBox Width="60" Text="{Binding AoValue}"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WriteAoCommand}"/>
                </StackPanel>
            </Border>

            <!-- 扩展数字信号 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="3">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="扩展数字信号 (RI/RO/UI/UO/SI/SO/WI/WO/WSI/WSO)"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="类型 "/>
                        <ComboBox Width="80" SelectedIndex="{Binding ExtDigTypeIndex}">
                            <ComboBoxItem Content="RI"/><ComboBoxItem Content="RO"/>
                            <ComboBoxItem Content="UI"/><ComboBoxItem Content="UO"/>
                            <ComboBoxItem Content="SI"/><ComboBoxItem Content="SO"/>
                            <ComboBoxItem Content="WI"/><ComboBoxItem Content="WO"/>
                            <ComboBoxItem Content="WSI"/><ComboBoxItem Content="WSO"/>
                        </ComboBox>
                        <TextBlock Style="{StaticResource FieldLabel}" Text=" 索引 "/>
                        <TextBox Width="60" Text="{Binding ExtDigIndex}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadExtDigCommand}"/>
                        <Button Style="{StaticResource DefaultButton}" Content="写 ON" Command="{Binding WriteExtDigCommand}" CommandParameter="ON"/>
                        <Button Style="{StaticResource DefaultButton}" Content="写 OFF" Command="{Binding WriteExtDigCommand}" CommandParameter="OFF"/>
                        <TextBlock Text="{Binding ExtDigResult}" FontWeight="Bold" Foreground="#00BCD4"
                                   VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <TextBlock Text="提示: SI/SO 索引从 0 开始，其余从 1 开始。" FontSize="10" Foreground="#888"/>
                </StackPanel>
            </Border>

            <!-- 组信号 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="4">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="组信号 (GI/GO, 16位)"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="读取 GI["/>
                        <TextBox Width="40" Text="{Binding GiIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] →"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadGiCommand}"/>
                        <TextBlock Text="{Binding GiResult}" FontWeight="Bold" Foreground="#795548" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="写入 GO["/>
                        <TextBox Width="40" Text="{Binding GoIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] = "/>
                        <TextBox Width="60" Text="{Binding GoValue}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="写入" Command="{Binding WriteGoCommand}"/>
                        <Button Style="{StaticResource DefaultButton}" Content="读取" Command="{Binding ReadGoCommand}"/>
                        <TextBlock Text="{Binding GoResult}" FontWeight="Bold" Foreground="#795548" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- PMC -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="5">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="PMC 信号 (R/K 继电器, D 数据)"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="区 "/>
                        <ComboBox Width="60" SelectedIndex="{Binding PmcZoneIndex}">
                            <ComboBoxItem Content="R"/>
                            <ComboBoxItem Content="K"/>
                        </ComboBox>
                        <TextBlock Style="{StaticResource FieldLabel}" Text=" 索引 "/>
                        <TextBox Width="40" Text="{Binding PmcIndex}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadPmcCommand}"/>
                        <Button Style="{StaticResource DefaultButton}" Content="写 ON" Command="{Binding WritePmcCommand}" CommandParameter="ON"/>
                        <Button Style="{StaticResource DefaultButton}" Content="写 OFF" Command="{Binding WritePmcCommand}" CommandParameter="OFF"/>
                        <TextBlock Text="{Binding PmcResult}" FontWeight="Bold" Foreground="#3F51B5" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="D 数据 D["/>
                        <TextBox Width="40" Text="{Binding PmcDataIndex}"/>
                        <TextBlock Style="{StaticResource FieldLabel}" Text="] = "/>
                        <TextBox Width="60" Text="{Binding PmcDataValue}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="写 D" Command="{Binding WritePmcDataCommand}"/>
                        <Button Style="{StaticResource DefaultButton}" Content="读 D" Command="{Binding ReadPmcDataCommand}"/>
                        <TextBlock Text="{Binding PmcDataResult}" FontWeight="Bold" Foreground="#3F51B5" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
