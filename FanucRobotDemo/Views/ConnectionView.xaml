<UserControl x:Class="FanucRobotDemoSH.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="1000">
    <StackPanel Orientation="Horizontal">
        <Button Style="{StaticResource DefaultButton}" Content="📖 读取 R[1]" Width="100" Command="{Binding QuickReadR1Command}"/>
        <Button Style="{StaticResource DefaultButton}" Content="📖 读取当前关节坐标" Width="150" Command="{Binding QuickReadJointCommand}"/>
        <Button Style="{StaticResource DefaultButton}" Content="📖 读取 DI[1-8]" Width="110" Command="{Binding QuickReadDiCommand}"/>
        <Button Style="{StaticResource DefaultButton}" Content="📖 读取任务状态" Width="110" Command="{Binding QuickReadTaskCommand}"/>
        <Button Style="{StaticResource DefaultButton}" Content="📖 读取当前报警" Width="110" Command="{Binding QuickReadAlarmCommand}"/>
    </StackPanel>
</UserControl>
