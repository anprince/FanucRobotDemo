<UserControl x:Class="FanucRobotDemoSH.Views.TaskAlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1000"
             Background="{StaticResource AppBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 任务 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="任务监控"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="监控任务 "/>
                        <TextBox Width="40" Text="{Binding TaskIndex}"/>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadTaskCommand}"/>
                    </StackPanel>
                    <Border Background="#F5F5F5" Padding="8">
                        <TextBlock Text="{Binding TaskResult}" FontFamily="Consolas" FontSize="12"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- 报警 -->
            <Border Style="{StaticResource CardBorder}" Grid.Row="1">
                <StackPanel>
                    <TextBlock Style="{StaticResource CardTitle}" Text="报警管理"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Style="{StaticResource FieldLabel}" Text="读取条数 "/>
                        <TextBox Width="40" Text="{Binding AlarmCount}"/>
                        <ComboBox Width="90" SelectedIndex="{Binding AlarmTypeIndex}">
                            <ComboBoxItem Content="历史(Full)"/>
                            <ComboBoxItem Content="当前(Short)"/>
                            <ComboBoxItem Content="当前(Full)"/>
                        </ComboBox>
                        <Button Style="{StaticResource PrimaryButton}" Content="读取" Command="{Binding ReadAlarmCommand}"/>
                        <Button Style="{StaticResource DangerButton}" Content="清除当前报警" Command="{Binding ClearAlarmCommand}"/>
                    </StackPanel>
                    <Border Background="#F5F5F5" Padding="8" MaxHeight="220">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBlock Text="{Binding AlarmResult}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</UserControl>
